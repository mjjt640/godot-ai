# P0 Runtime Operation Tools - Implementation Plan

> **For agentic workers:** Use superpowers:subagent-driven-development or superpowers:executing-plans.

**Goal:** Add 8 runtime operation tools (signal control, physics, 3D, navigation).
**Architecture:** Each tool generates GDScript and executes via executeGdscript(). Module registers in toolModules.
**Tech Stack:** TypeScript, Node.js test runner, GDScript generation.
**Spec:** docs/superpowers/specs/2026-05-02-godot-ops-p0-design.md v3

---

## File Structure

| File | Action | Responsibility |
|------|--------|---------------|
| src/tools/godot-ops.ts | Create | Tool definitions, handlers, helpers, GDScript generators |
| src/GodotServer.ts | Modify L27-29 | Register new module in toolModules |
| test/godot-ops.test.js | Create | Tests for helpers, generators, edge cases |
| README.md | Modify | Update tool count and list |

---

### Task 1: Helper Utilities + Tests

- [ ] **Step 1:** Create test/godot-ops.test.js with tests for normalizeNodePath (6 cases), gdEscape (7 cases), validateVector3 (6 cases), TYPE_WHITELIST (5 cases), ERROR_CODES (6 cases)
- [ ] **Step 2:** Run npm run build && npm test - expect FAIL
- [ ] **Step 3:** Create src/tools/godot-ops.ts with exports: normalizeNodePath, gdEscape, validateVector3, TYPE_WHITELIST (16 types), ERROR_CODES (6 codes). Add placeholder getToolDefinitions/handleTool for build.
- [ ] **Step 4:** Run npm run build && npm test - expect ALL PASS
- [ ] **Step 5:** git commit -m "feat(godot-ops): add helper utilities with tests"

### Task 2: Signal GDScript Generators + Tests

- [ ] **Step 1:** Add tests for genSignalConnectScript, genSignalDisconnectScript, genSignalEmitScript (4 cases: no args, numbers, strings, reject objects), genSignalListScript
- [ ] **Step 2:** Run npm run build && npm test - expect FAIL
- [ ] **Step 3:** Implement 4 signal generators. Each outputs extends SceneTree with node-not-found guards, _mcp_output, quit().
- [ ] **Step 4:** Run npm run build && npm test - expect ALL PASS
- [ ] **Step 5:** git commit -m "feat(godot-ops): add signal GDScript generators with tests"

### Task 3: Physics/3D/Nav Generators + Tests

- [ ] **Step 1:** Add tests for genRaycastScript (basic + exclude), genBodyInfoScript, genCreate3DScript (position + scale), genNavQueryScript
- [ ] **Step 2:** Run npm run build && npm test - expect FAIL
- [ ] **Step 3:** Implement genRaycastScript (Godot 4 PhysicsRayQueryParameters3D + exclude), genBodyInfoScript (CollisionShape3D scan, has_collision:false), genCreate3DScript (position/rotation/scale/props), genNavQueryScript (NavigationServer3D.map_get_path)
- [ ] **Step 4:** Run npm run build && npm test - expect ALL PASS
- [ ] **Step 5:** git commit -m "feat(godot-ops): add physics/3D/nav generators with tests"

### Task 4: Tool Definitions + Handler + Registration

- [ ] **Step 1:** Implement getToolDefinitions() with 8 tool schemas. Vector3 as {x,y,z}. NodePath not res://. Descriptions note runtime non-persistence. Optional load_autoloads param.
- [ ] **Step 2:** Implement handleTool() with 8 cases: validate params (normalizeNodePath, gdEscape, validateVector3, TYPE_WHITELIST), generate GDScript, call executeGdscript(), return unified {success,data,error,error_code,warnings}.
- [ ] **Step 3:** Register in GodotServer.ts: import * as godotOps and add to toolModules array.
- [ ] **Step 4:** Run npm run build && npm test - expect ALL PASS
- [ ] **Step 5:** git commit -m "feat(godot-ops): add 8 runtime operation tools with handler and registration"

### Task 5: README Update + Final Verification

- [ ] **Step 1:** Update README: tool count 35->43, add runtime tools section, add load_autoloads env var, add non-persistence warning.
- [ ] **Step 2:** Run npm run build && npm test - expect ALL PASS
- [ ] **Step 3:** git commit -m "docs: update README for 8 new runtime tools" && git push